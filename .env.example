# Copy to .env and fill in. .env is gitignored — never commit a real key.
OPENAI_API_KEY=sk-your-key-here

# Optional overrides
SYNTHGEN_MODEL=gpt-4o-mini
SYNTHGEN_TEMPERATURE=1.0
SYNTHGEN_BATCH_SIZE=25
SYNTHGEN_CONCURRENCY=4
SYNTHGEN_MAX_RETRIES=3
SYNTHGEN_TIMEOUT=60
